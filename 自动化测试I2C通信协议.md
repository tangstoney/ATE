# 自动化测试通讯协议

**协议版本**：1.0  
**日期**：2026-03-27  
**说明**：本文档定义了主机与从机之间通过串口进行通信的帧格式、命令及状态码，并提供了多种通信示例。所有多字节字段均采用**大端模式**（高位字节在前）传输。

---

## 一、概述

本协议采用主从式通信，主机设备 ID 固定为 **0x01**，从机设备 ID 范围为 **0x02~0xFF**。通信帧分为两种基本格式：

- **通用帧**：用于请求、响应和事件上报，包含数据长度和数据字段。
- **响应状态帧**：从机专用，用于返回命令执行状态，无数据字段，直接携带状态码。

每帧数据以固定的帧头 `0xAA55` 开始，以 16 位 CRC 校验结束，校验范围从设备 ID 字段到数据（或状态）字段的最后一个字节。

---

## 二、帧格式定义

### 2.1 通用帧格式（主机请求、从机响应、从机事件上报）

| 字段      | 长度（字节） | 描述                                                         | 取值示例                 |
|-----------|--------------|--------------------------------------------------------------|--------------------------|
| 帧头      | 2            | 固定为 `0xAA55`，先发送 `0xAA`，再发送 `0x55`                 | 0xAA, 0x55               |
| 设备 ID   | 1            | 标识发送方或目标设备                                         | 主机：0x01；从机：0x02~0xFF |
| 帧类型    | 1            | `0x01`：请求<br>`0x02`：响应<br>`0x03`：事件上报             | 0x01                     |
| 命令      | 1            | 具体操作码，详见第四章                                       | 0x01（读取版本号）       |
| 数据长度  | 2            | 数据字段的字节数，范围 `0x0000`~`0xFFFF`，高位在前            | 0x0001                   |
| 数据      | N            | 命令相关的数据内容，N 等于数据长度                            | 可变                     |
| 校验 CRC16| 2            | CRC16 校验值，校验范围从设备 ID 到数据结束，高位在前          | 0xXXXX                   |

### 2.2 响应状态帧格式（从机专用）

| 字段      | 长度（字节） | 描述                                                         | 取值示例                 |
|-----------|--------------|--------------------------------------------------------------|--------------------------|
| 帧头      | 2            | 固定为 `0xAA55`                                               | 0xAA, 0x55               |
| 设备 ID   | 1            | 从机自身 ID                                                   | 0x02                     |
| 帧类型    | 1            | 固定 `0x04`：响应状态                                         | 0x04                     |
| 命令      | 1            | 对应请求的命令码                                             | 0x01                     |
| 状态      | 1            | 命令执行结果状态码，详见第五章                               | 0x00（成功）             |
| 校验 CRC16| 2            | CRC16 校验值，校验范围从设备 ID 到状态结束，高位在前          | 0xXXXX                   |

---

## 三、字段说明

- **帧头**：固定 2 字节 `0xAA55`，用于同步。
- **设备 ID**：主机发送时主机ID（0x01）时，所有从机需要响应，主机发送目标从机 ID时，只有目标从机响应；从机发送时为本机 ID。
- **帧类型**：区分通信方向及类型。
- **命令**：定义具体操作，系统命令 `0x00~0x1F`，业务命令 `0x20~0x7F`，保留 `0x80~0xFF`。
- **数据长度**：仅通用帧有效，指示数据域字节数。
- **数据**：可变长度，内容与命令相关。
- **状态**：仅响应状态帧有效，指示处理结果。
- **校验**：CRC16-CCITT（多项式 `0x1021`），初始值 `0xFFFF`，计算范围不包括帧头。

---

## 四、命令码定义

| 命令码   | 类型       | 描述                         | 数据说明                     |
|----------|------------|------------------------------|------------------------------|
| [0x20](#cmd-20) | 业务命令   | 按键 GPIO 测试               | 按键 GPIO 输出数据 |
| [0x21](#cmd-21) | 业务命令   | 左旋钮测试                   | 1 字节动作码：左旋/右旋/按压 |
| [0x22](#cmd-22) | 业务命令   | 右旋钮测试                   | 1 字节动作码：左旋/右旋/按压 |
| [0x23](#cmd-23) | 业务命令   | 中间旋钮测试                 | 1 字节动作码：左旋/右旋/按压 |
| [0x30](#cmd-30) | 业务命令   | 无线充状态设置               | 1 字节状态值：0x03/0x04 |
| [0x31](#cmd-31) | 业务命令   | NMOS 状态设置                | 1 字节状态值：0x00/0x01 |
| [0x32](#cmd-32) | 业务命令   | 无线充电流读取               | 请求无 Payload；响应 2 字节电流寄存器值 |
| [0x40](#cmd-40) | 业务命令   | 扬声器 SPK_LN ADC 读取       | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x41](#cmd-41) | 业务命令   | 扬声器 SPK_LP ADC 读取       | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x42](#cmd-42) | 业务命令   | 扬声器 SPK_RN ADC 读取       | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x43](#cmd-43) | 业务命令   | 扬声器 SPK_RP ADC 读取       | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x44](#cmd-44) | 业务命令   | 扬声器 HP_L ADC 读取         | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x45](#cmd-45) | 业务命令   | 扬声器 HP_R ADC 读取         | 请求无 Payload；响应 2 字节 ADC 平均值 |
| [0x46](#cmd-46) | 业务命令   | 扬声器 PDM 频率读取          | 请求无 Payload；响应 4 字节频率整数值 |

---

## 五、状态码定义（用于响应状态帧）

| 状态码 | 描述               | 说明                         |
|--------|--------------------|------------------------------|
| 0x00   | 成功               | 命令执行成功                 |
| 0x01   | 失败               | 命令执行失败                 |
| 0x02   | 帧头错误           | 接收帧帧头不是 `0xAA55`      |
| 0x03   | 帧类型错误         | 不支持的帧类型               |
| 0x04   | 命令错误           | 命令码未定义或不被支持       |
| 0x05   | 长度错误           | 数据长度超出允许范围         |
| 0x06   | 数据参数错误       | 数据内容无效                 |
| 0x07   | CRC 错误           | 接收帧 CRC 校验错误          |
| 0x08   | Busy               | 设备忙                       |
| 0x09   | Processing         | 正在处理中                   |
| 0x0A   | Resource busy      | 资源忙                       |

---

## 六、通信命令格式


所有数据均为十六进制表示，`??` 表示需按实际内容计算的 CRC 字节。

协议说明：

- 请求帧的帧类型固定为 `0x01`。
- 从机回包包含**响应帧**和**状态帧**，响应帧帧类型固定为 `0x02`，状态帧帧类型固定为 `0x04`。
- Payload 最大长度为 `10` 字节。
- 成功时返回响应帧，失败时返回状态帧。

<a id="cmd-20"></a>
### 6.20 `0x20` 按键 GPIO 测试

**功能说明**：用于配置按键 GPIO 测试输出数据。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x02           | 示例目标从机 ID |
| 3        | 帧类型   | 0x01           | 请求 |
| 4        | 命令     | 0x20           | 按键 GPIO 测试 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x03           | 低字节，示例长度为 3 |
| 7        | 数据     | 0x12           | 第 1 个输出字节 |
| 8        | 数据     | 0x34           | 第 2 个输出字节 |
| 9        | 数据     | 0x56           | 第 3 个输出字节 |
| 10       | 校验 CRC16 | 0x??         | 高字节 |
| 11       | 校验 CRC16 | 0x??         | 低字节 |

说明：

- `0x20` 的数据长度可按实际需要设置，范围为 `1~10` 字节。
- 常见用法是发送 `3` 字节按键测试数据。

**请求示例**：`AA 55 02 01 20 00 03 12 34 56 ?? ??`

**状态响应格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 固定返回值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x20           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**响应示例**：`AA 55 01 04 20 00 ?? ??`

<a id="cmd-21"></a>
### 6.21 `0x21` 左旋钮测试

**功能说明**：控制左旋钮执行一步动作。Payload 固定为 `1` 字节动作码。

**动作码定义**：

| 动作码 | 含义 |
|--------|------|
| 0x00   | 无动作 |
| 0x01   | 左旋 |
| 0x02   | 右旋 |
| 0x03   | 按压 |

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x02           | 示例目标从机 ID |
| 3        | 帧类型   | 0x01           | 请求 |
| 4        | 命令     | 0x21           | 左旋钮测试 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x01           | 低字节，固定为 1 |
| 7        | 数据     | 0x01           | 示例：左旋 |
| 8        | 校验 CRC16 | 0x??         | 高字节 |
| 9        | 校验 CRC16 | 0x??         | 低字节 |

**请求示例**：`AA 55 02 01 21 00 01 01 ?? ??`

**状态响应格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 固定返回值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x21           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**响应示例**：`AA 55 01 04 21 00 ?? ??`

<a id="cmd-22"></a>
### 6.22 `0x22` 右旋钮测试

**功能说明**：控制右旋钮执行一步动作。Payload 固定为 `1` 字节动作码，动作码定义同 `6.21`。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x02           | 示例目标从机 ID |
| 3        | 帧类型   | 0x01           | 请求 |
| 4        | 命令     | 0x22           | 右旋钮测试 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x01           | 低字节，固定为 1 |
| 7        | 数据     | 0x02           | 示例：右旋 |
| 8        | 校验 CRC16 | 0x??         | 高字节 |
| 9        | 校验 CRC16 | 0x??         | 低字节 |

**请求示例**：`AA 55 02 01 22 00 01 02 ?? ??`

**状态响应格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 固定返回值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x22           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**响应示例**：`AA 55 01 04 22 00 ?? ??`

<a id="cmd-23"></a>
### 6.23 `0x23` 中间旋钮测试

**功能说明**：控制中间旋钮执行一步动作。Payload 固定为 `1` 字节动作码，动作码定义同 `6.21`。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x02           | 示例目标从机 ID |
| 3        | 帧类型   | 0x01           | 请求 |
| 4        | 命令     | 0x23           | 中间旋钮测试 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x01           | 低字节，固定为 1 |
| 7        | 数据     | 0x03           | 示例：按压 |
| 8        | 校验 CRC16 | 0x??         | 高字节 |
| 9        | 校验 CRC16 | 0x??         | 低字节 |

**请求示例**：`AA 55 02 01 23 00 01 03 ?? ??`

**状态响应格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 固定返回值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x23           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**响应示例**：`AA 55 01 04 23 00 ?? ??`

<a id="cmd-30"></a>
### 6.30 `0x30` 无线充状态设置

**功能说明**：设置无线充电状态。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x01           | 请求帧 |
| 4        | 命令     | 0x30           | 无线充状态设置 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x01           | 低字节，Payload 长度为 1 |
| 7        | 数据     | 0x03 / 0x04    | 0x03=不充电，0x04=充电 |
| 8        | 校验 CRC16 | 0x??         | 高字节 |
| 9        | 校验 CRC16 | 0x??         | 低字节 |

**请求示例**：`AA 55 01 01 30 00 01 04 ?? ??`

**状态帧格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x30           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**状态示例**：`AA 55 01 04 30 00 ?? ??`

<a id="cmd-31"></a>
### 6.31 `0x31` NMOS 状态设置

**功能说明**：设置 NMOS 开关状态。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x01           | 请求帧 |
| 4        | 命令     | 0x31           | NMOS 状态设置 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x01           | 低字节，Payload 长度为 1 |
| 7        | 数据     | 0x00 / 0x01    | 0x00=关闭，0x01=打开 |
| 8        | 校验 CRC16 | 0x??         | 高字节 |
| 9        | 校验 CRC16 | 0x??         | 低字节 |

**请求示例**：`AA 55 01 01 31 00 01 01 ?? ??`

**状态帧格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x04           | 状态帧 |
| 4        | 命令     | 0x31           | 对应请求命令 |
| 5        | 状态     | 0x00           | 示例为成功 |
| 6        | 校验 CRC16 | 0x??         | 高字节 |
| 7        | 校验 CRC16 | 0x??         | 低字节 |

**状态示例**：`AA 55 01 04 31 00 ?? ??`

<a id="cmd-32"></a>
### 6.32 `0x32` 无线充电流读取

**功能说明**：读取无线充电电流值。

**请求格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x01           | 请求帧 |
| 4        | 命令     | 0x32           | 无线充电流读取 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x00           | 低字节，Payload 长度为 0 |
| 7        | 校验 CRC16 | 0x??         | 高字节 |
| 8        | 校验 CRC16 | 0x??         | 低字节 |

**读取说明**：

- 固件读取电流检测芯片的电流寄存器，寄存器地址为 `0x04`。
- 成功时返回 2 字节响应数据。

**请求示例**：`AA 55 01 01 32 00 00 ?? ??`

**响应格式**：

| 字节索引 | 字段     | 值（十六进制） | 备注 |
|----------|----------|----------------|------|
| 0        | 帧头     | 0xAA           | 固定 |
| 1        | 帧头     | 0x55           | 固定 |
| 2        | 设备 ID  | 0x01           | 当前代码固定值 |
| 3        | 帧类型   | 0x02           | 响应帧 |
| 4        | 命令     | 0x32           | 对应请求命令 |
| 5        | 数据长度 | 0x00           | 高字节 |
| 6        | 数据长度 | 0x02           | 低字节，固定为 2 |
| 7        | 数据     | 读回值高字节   | INA226 CURRENT 高字节 |
| 8        | 数据     | 读回值低字节   | INA226 CURRENT 低字节 |
| 9        | 校验 CRC16 | 0x??         | 高字节 |
| 10       | 校验 CRC16 | 0x??         | 低字节 |

**响应示例**：`AA 55 01 02 32 00 02 XX XX ?? ??`

<a id="cmd-40"></a>
### 6.40 `0x40` 扬声器 `SPK_LN` ADC 读取

**功能说明**：读取 `SPK_LN` 通道 ADC 平均值。

**请求格式**：

| 字节索引 | 字段       | 值（十六进制） | 备注 |
|----------|------------|----------------|------|
| 0        | 帧头       | 0xAA           | 固定 |
| 1        | 帧头       | 0x55           | 固定 |
| 2        | 设备 ID    | 0x02           | 当前 Speaker 固件设备 ID |
| 3        | 帧类型     | 0x01           | 请求帧 |
| 4        | 命令       | 0x40           | `SPK_LN` ADC 读取 |
| 5        | 数据长度   | 0x00           | 高字节 |
| 6        | 数据长度   | 0x00           | 低字节，固定为 0 |
| 7        | 校验 CRC16 | 0x??           | 高字节 |
| 8        | 校验 CRC16 | 0x??           | 低字节 |

**请求示例**：`AA 55 02 01 40 00 00 ?? ??`

**响应格式**：

| 字节索引 | 字段       | 值（十六进制） | 备注 |
|----------|------------|----------------|------|
| 0        | 帧头       | 0xAA           | 固定 |
| 1        | 帧头       | 0x55           | 固定 |
| 2        | 设备 ID    | 0x02           | 当前 Speaker 固件设备 ID |
| 3        | 帧类型     | 0x02           | 响应帧 |
| 4        | 命令       | 0x40           | 对应请求命令 |
| 5        | 数据长度   | 0x00           | 高字节 |
| 6        | 数据长度   | 0x02           | 低字节，固定为 2 |
| 7        | 数据       | ADC 高字节     | ADC 平均值高字节 |
| 8        | 数据       | ADC 低字节     | ADC 平均值低字节 |
| 9        | 校验 CRC16 | 0x??           | 高字节 |
| 10       | 校验 CRC16 | 0x??           | 低字节 |

说明：

- ADC 结果按大端模式返回，即 `Payload[0]` 为高字节、`Payload[1]` 为低字节。
- 若请求帧 `Data_Len` 不为 `0x0000`，返回状态帧，状态码为 `0x06`。
- 若 ADC 读取失败，返回状态帧，状态码为 `0x01`。

**响应示例**：`AA 55 02 02 40 00 02 XX XX ?? ??`

<a id="cmd-41"></a>
### 6.41 `0x41` 扬声器 `SPK_LP` ADC 读取

格式与 `6.40` 相同，仅命令字改为 `0x41`，返回 `SPK_LP` 通道 ADC 平均值。

- 请求示例：`AA 55 02 01 41 00 00 ?? ??`
- 响应示例：`AA 55 02 02 41 00 02 XX XX ?? ??`

<a id="cmd-42"></a>
### 6.42 `0x42` 扬声器 `SPK_RN` ADC 读取

格式与 `6.40` 相同，仅命令字改为 `0x42`，返回 `SPK_RN` 通道 ADC 平均值。

- 请求示例：`AA 55 02 01 42 00 00 ?? ??`
- 响应示例：`AA 55 02 02 42 00 02 XX XX ?? ??`

<a id="cmd-43"></a>
### 6.43 `0x43` 扬声器 `SPK_RP` ADC 读取

格式与 `6.40` 相同，仅命令字改为 `0x43`，返回 `SPK_RP` 通道 ADC 平均值。

- 请求示例：`AA 55 02 01 43 00 00 ?? ??`
- 响应示例：`AA 55 02 02 43 00 02 XX XX ?? ??`

<a id="cmd-44"></a>
### 6.44 `0x44` 扬声器 `HP_L` ADC 读取

格式与 `6.40` 相同，仅命令字改为 `0x44`，返回 `HP_L` 通道 ADC 平均值。

- 请求示例：`AA 55 02 01 44 00 00 ?? ??`
- 响应示例：`AA 55 02 02 44 00 02 XX XX ?? ??`

<a id="cmd-45"></a>
### 6.45 `0x45` 扬声器 `HP_R` ADC 读取

格式与 `6.40` 相同，仅命令字改为 `0x45`，返回 `HP_R` 通道 ADC 平均值。

- 请求示例：`AA 55 02 01 45 00 00 ?? ??`
- 响应示例：`AA 55 02 02 45 00 02 XX XX ?? ??`

<a id="cmd-46"></a>
### 6.46 `0x46` 扬声器 `PDM` 频率读取

**功能说明**：读取当前 `PDM` 音频主频值。

**请求格式**：

| 字节索引 | 字段       | 值（十六进制） | 备注 |
|----------|------------|----------------|------|
| 0        | 帧头       | 0xAA           | 固定 |
| 1        | 帧头       | 0x55           | 固定 |
| 2        | 设备 ID    | 0x02           | 当前 Speaker 固件设备 ID |
| 3        | 帧类型     | 0x01           | 请求帧 |
| 4        | 命令       | 0x46           | `PDM` 频率读取 |
| 5        | 数据长度   | 0x00           | 高字节 |
| 6        | 数据长度   | 0x00           | 低字节，固定为 0 |
| 7        | 校验 CRC16 | 0x??           | 高字节 |
| 8        | 校验 CRC16 | 0x??           | 低字节 |

**请求示例**：`AA 55 02 01 46 00 00 ?? ??`

**响应格式**：

| 字节索引 | 字段       | 值（十六进制） | 备注 |
|----------|------------|----------------|------|
| 0        | 帧头       | 0xAA           | 固定 |
| 1        | 帧头       | 0x55           | 固定 |
| 2        | 设备 ID    | 0x02           | 当前 Speaker 固件设备 ID |
| 3        | 帧类型     | 0x02           | 响应帧 |
| 4        | 命令       | 0x46           | 对应请求命令 |
| 5        | 数据长度   | 0x00           | 高字节 |
| 6        | 数据长度   | 0x04           | 低字节，固定为 4 |
| 7        | 数据       | 频率[31:24]    | 频率整数值最高字节 |
| 8        | 数据       | 频率[23:16]    | 频率整数值次高字节 |
| 9        | 数据       | 频率[15:8]     | 频率整数值次低字节 |
| 10       | 数据       | 频率[7:0]      | 频率整数值最低字节 |
| 11       | 校验 CRC16 | 0x??           | 高字节 |
| 12       | 校验 CRC16 | 0x??           | 低字节 |

说明：

- 固件先调用 `Speaker_Check_PDM_Get_Frequency()` 获取浮点频率值，再按 `Hz` 四舍五入为 `uint32_t` 后回传。
- 若频率值小于 `0`，则回传 `0x00000000`。
- 返回值按大端模式传输，例如 `1000 Hz` 对应 `00 00 03 E8`。
- 若请求帧 `Data_Len` 不为 `0x0000`，返回状态帧，状态码为 `0x06`。
- 若频率读取失败，返回状态帧，状态码为 `0x01`。

**响应示例**：`AA 55 02 02 46 00 04 00 00 03 E8 ?? ??`
